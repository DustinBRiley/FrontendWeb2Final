import React from 'react'

const CheckoutCart = () => {
  return (
    <div>
        {/* cartList .map{CartItem}, scroll bar if too tall so fixed height */}
    </div>
  )
}

export default CheckoutCart