import React from 'react'

const CardInfo = () => {
  return (
    <div>
        {/* Card number, CCV, exp date, zip, purchase button (saves last four of card number, cartList, date, to database for order history) */}
    </div>
  )
}

export default CardInfo