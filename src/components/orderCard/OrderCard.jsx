import React from 'react'

const OrderCard = () => {
  return (
    <div>
        {/* date of purchase, list of items .map{CartItem} should have the same css, card used with ending digits, total */}
    </div>
  )
}

export default OrderCard